{
  "name": "@cura/cli",
  "version": "0.3.0",
  "private": true,
  "description": "Cura dev-env CLI (Node.js + TypeScript). Standalone ollama-backed git/workflow CLI plus docker-compose helpers — no che-cli, no gh, no python.",
  "type": "module",
  "bin": {
    "cura": "dist/main.js"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "tsc",
    "start": "node dist/main.js"
  },
  "files": [
    "dist",
    "README.md",
    "SPEC.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
