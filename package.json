{
  "name": "@cura/cli",
  "version": "0.3.0",
  "private": true,
  "description": "Cura dev-env CLI (Node.js + TypeScript). Cura-specific helpers: docker-compose stack, ollama setup, GCP Cloud Run control, .che/workflows runner. No git/GitHub commands (use che-cli for that). Pure Node — no python, no che-cli, no js-yaml.",
  "type": "module",
  "bin": {
    "cura": "dist/main.js"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "tsc",
    "start": "node dist/main.js"
  },
  "files": [
    "dist",
    "README.md",
    "SPEC.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
